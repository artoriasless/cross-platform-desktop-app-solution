import { IAppConfig } from '../interfaces';

const defaultAppConfig: IAppConfig = {
  size: {
    width: 1000,
    height: 720,
    minWidth: 400,
    minHeight: 300,
  },
};

export default defaultAppConfig;
